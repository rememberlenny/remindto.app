%div.row.dashboards{style: 'postion: relative'}

  %nav.top-bar{style: "", data: {topbar: '', options: "custom_back_text: false"}}
    %section.top-bar-section
      %ul.right
        %li
          = link_to "View", user_home_path, { data: { button: "view"} }
        %li
          = link_to "Edit", edit_account_path(current_user.account_id), { data: { button: "edit"} }
        / %li= link_to "Stats", stats_account_path(current_user.account_id)
        %li
          = link_to "Signups", signups_account_path(current_user.account_id), { data: { button: "signups"} }
        %li
          = link_to "Integrations", integrations_account_path(current_user.account_id), { data: { button: "integrations"} }
